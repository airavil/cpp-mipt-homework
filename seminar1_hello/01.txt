1) нет #include <stdio.h> принтф не сработал бы
2) в сканф нет & перед а
3) короч, под ифом выражение "a = 10", "=" - оператор он выдает значение a, по идеи если a = 0, то программа выведет No. Нужно вместо "=" "=="
4) нужно убрать \n в сканфе
5) нужно два принтфа запихнуть в 1 блок {}. тут иф отвечает только за 1 строку с принтфом
6) после ифа не нужно ";"
7) тут лучше использовать цикл фор то есть вместо "while (i <= 10)" написать "for(i; i<=10; i++)"
8) аналогично с 6), после вайла не нужно ";"
9) нужно написать int sum = 0
10) у нас i гуляет до 5, а конец массива 4. Ошибка приводит к неопределённому поведению.